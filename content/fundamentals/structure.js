const path = require('path');
const here = `${path.parse(__dirname).base}${path.sep}`;
module.exports = [
    `${here}introduction`,
    `${here}blockchain-basics`,
    `${here}data-bases`,
    `${here}blockchain-history`,
    `${here}consensus`,
    `${here}blockchain-definitions`,
    `${here}cryptography`
];
